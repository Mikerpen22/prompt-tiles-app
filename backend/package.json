{
  "name": "prompt-tiles-api",
  "version": "1.0.0",
  "description": "Prompt Tiles API using Cloudflare Workers and D1",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "init-db": "wrangler d1 execute prompt_tiles_db --local --file=./schema.sql"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@hono/zod-validator": "^0.1.11",
    "hono": "^3.11.7",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "wrangler": "^3.22.1"
  }
}
